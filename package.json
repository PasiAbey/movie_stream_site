{
  "name": "movie-streamer-root",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack movie streamer with React frontend and Node backend.",
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend start"
  }
}
